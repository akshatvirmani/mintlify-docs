---
title: 'Installation'
description: 'Before you start using the ```useGitHub``` hook, ensure you have ```axios``` installed in your project, as this hook relies on it to make HTTP requests'
---

``` htm
npm install axios
```

After installing ```axios```, run the command below to use ```useGitHub```.

``` htm
npm install use-github-react
```